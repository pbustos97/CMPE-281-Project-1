import React from "react";
import Header from "../components/Header";

function HomePage() {
    return (
        <div>
            <Header />
            <h1>Use the header to navigate through the app.</h1>
        </div>
    )
}

export default HomePage;